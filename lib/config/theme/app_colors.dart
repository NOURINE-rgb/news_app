import 'package:flutter/material.dart';

class AppColors {
  // i will change them later this just a dummy data of how i s tructure my colors
  static const Color blueColor = Color(0xFF2D5BD0);
  static const Color primaryColorLight = Color(0xFF231F20);
  static const Color primaryColorDark = Color(0xFFE0DCDD);
  static const Color backgroundLight = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFF0D0D0D);
  static const Color textColorLight = Color(0xFF6D6265);
  static const Color textColorDark = Color(0xFF9B8A8F);
}
